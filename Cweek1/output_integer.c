#include <stdio.h>

// outputting an `int` type

int main() {

  int x = 3;

  // note that `printf` is a lot like `System.out.printf` in Java, you
  // can use the formatting code `%d` for `int` types, and `\n` to
  // start a new line..
  printf("x = %d\n", x);

  return 0;
}
